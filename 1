#include "main.h"
#include <stdio.h>
/**
 * main - printing a prompt and reading
 * @bc: integer
 * @argv: arguments
 * Return: Always 0
 */
int main(int bc, char **argv)
{
char *prompt = "(simpleshell) $ ";
/* variables for typed info and size allocation */
char *typed;
size_t m = 0;
ssize_t mchars_re = 0;
/* void variables */
(void)bc;
(void)argv;
/* creating a loop */
while (1)
{
printf("%s", prompt);
mchars_re = getline(&typed, &m, stdin);
/* check if function failed or EOF(Ctrl+D) */
if (mchars_re == -1)
{
	printf("Exiting simpleshell....\n");
	return (-1);
}
/* to get what user types */
/* getline(&typed, &m, stdin); */
printf("%s\n", typed);
free(typed);
}
return (0);
}
